# -----------------------------------------------------------------------------
# Float-to-Tile Global Configuration
# -----------------------------------------------------------------------------

# --- 1. Monitor Mapping (Stable Identifiers) ----------------------------------
# If left empty (""), the 'run.sh' script will automatically populate this list (e.g., HDMI-0, eDP-1-1).

MONITOR_MAP_NAMES=""